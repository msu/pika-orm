---
layout: default
title: "Validation & Forms"
description: "Bean validation and safe web-form binding in PikaORM: require, requireUnique, addError, saveOrThrow, and setFieldsFrom."
active_page: validation
permalink: /pages/validation/
---

# Validation & Forms

> Placeholder — content coming. Will cover bean validation (`require`, `requireUnique`, `addError`, `validate()` / `saveOrThrow()`) and safe web-form binding with `setFieldsFrom()`. Some of this currently lives on the [CRUD]({{ '/pages/crud/' | relative_url }}) page.
