---
layout: default
title: "Plain Java Objects"
description: "Use PikaORM directly with plain Java objects, no base class: orm.insert, orm.update, orm.query, and raw SQL."
active_page: plain-objects
permalink: /pages/plain-objects/
---

# Plain Java Objects

> Placeholder — content coming. Will cover using the ORM directly without extending `PikaBean`: `orm.insert(obj)`, `orm.update(obj)`, `orm.query(Class)`, and raw `orm.select(sql)`.
